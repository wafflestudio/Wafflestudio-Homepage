<header>
  <h1>Wafflestudio</h1>
  <h2>We design develop delicious WEB</h2>
</header>
<%= render :partial => 'layouts/navigation' %>
<article>
  <div id="tweetbox">
    <h3>@wafflestudio</h3>
		<a target="_blank" href="http://twitter.com/wafflestudio" title="Wafflestudio" id="follow_us"></a>
    <ul id="tweets">
    </ul>
  </div>
  <div id="carousel">
    <div id="slider">
    <% @carousels.each do |carousel| %>
      <%= link_to_function image_tag(carousel.c_image.url), "slider_action('#{carousel.action[:type]}', '#{carousel.action[:value]}')" %>
    <% end %>
    </div>
  </div>
</article>
